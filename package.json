{
  "name": "robot-controller",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "rm -rf ./js/etherealjs && git clone https://github.com/etherealdata/etherealjs ./js/etherealjs && npm install http-server pm2 -g",
    "persist": "pm2 start http-server -- -p 80 && pm2 save",
    "start": "http-server"
  },
  "author": "",
  "license": "ISC"
}
